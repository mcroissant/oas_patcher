overlay: 1.0.0
info:
  title: Security Overlay
  version: 1.0.0
actions:
  - target: "$.components"
    update:
      securitySchemes:
        BearerAuth:
          type: http
          scheme: bearer
          bearerFormat: JWT
  - target: "$.security"
    update:
      - BearerAuth: []
